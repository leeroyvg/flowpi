services:
  flowpi:
    build: .
    container_name: flowpi
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5000:5000"
      - "8000:8000"
    volumes:
      - ./data:/app/data
